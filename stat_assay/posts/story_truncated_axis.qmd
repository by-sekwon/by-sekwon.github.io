---
format:
  html:
    sidebar: stat_assay
title: "(그래프는 어떻게 우리를 속이는가) 막대그래프의 축을 자르면 차이가 커진다"
author: "권세혁"
date: "2026-08-10"
categories: [통계기초, 데이터시각화]
description: "2014년 지방선거 개표 방송에서 경기도지사 후보 두 명의 지지율 격차는 1.3%포인트였다. 그런데 화면 속 막대그래프에서는 그 차이가 두 배 넘게 벌어져 보였다. 축을 어디서부터 그리느냐의 문제다."
---

## 화면 속 막대는 두 배 차이였다

통계 시각화 수업 첫 시간에 나는 항상 옛날 선거 개표 방송 캡처 화면을 하나 보여준다. 학생들에게 묻는다. "이 두 후보, 지지율 차이가 얼마나 나 보여요?" 대부분 "한쪽이 거의 두 배는 앞선 것 같은데요"라고 답한다.

실제 수치를 알려주면 다들 놀란다. 2014년 지방선거 경기도지사 개표 방송에서, 두 후보의 실제 득표율 차이는 **1.3%포인트**였다. 남경필 36.0%, 김진표 34.7%. 그런데 그날 방송 화면의 막대그래프에서는 한쪽 막대가 다른 쪽보다 두 배 넘게 높게 그려졌다.

숫자를 조작한 것은 아니다. 그래프에 찍힌 36.0과 34.7이라는 숫자 자체는 정확했다. 문제는 **막대를 어디서부터 그렸는가**였다.

## 막대그래프의 약속: 높이는 곧 값이다

막대그래프가 성립하는 이유는 단순한 약속 하나 때문이다. 강의노트 [〈기초통계 1. 통계학의 개념 — 범주형 데이터〉](../../notes/intro_stat/data.qmd#범주형-데이터)는 막대그래프를 이렇게 정의한다. "각 범주에 해당하는 빈도 또는 비율을 막대의 **높이**로 나타낸다." 이 정의가 성립하려면 모든 막대의 밑변, 즉 축의 시작점이 같은 기준(보통 0)에서 출발해야 한다. 그래야 막대 높이의 비율이 실제 값의 비율과 일치한다.

축의 시작점을 0이 아닌 다른 값으로 옮기는 순간, 이 약속은 깨진다. 높이는 더 이상 값에 비례하지 않는다.

```{python}
#| echo: false
#| fig-cap: "The actual 2014 Gyeonggi governor race result — a 1.3-percentage-point gap, drawn honestly with the y-axis starting at 0%."
import matplotlib.pyplot as plt

candidates = ["Nam\n(36.0%)", "Kim\n(34.7%)"]
values = [36.0, 34.7]
colors = ["#1e3a2f", "#a8790a"]

fig, ax = plt.subplots(figsize=(6, 3.8))
bars = ax.bar(candidates, values, color=colors, width=0.45)
for b, v in zip(bars, values):
    ax.annotate(f"{v:.1f}%", xy=(b.get_x()+b.get_width()/2, v), xytext=(0, 5),
                textcoords="offset points", ha="center", fontsize=11)
ax.set_ylabel("Vote share (%)")
ax.set_ylim(0, 45)
ax.set_title("Axis starts at 0% — a genuinely close race", fontsize=10.5, color="#1e3a2f")
ax.spines[["top", "right"]].set_visible(False)
plt.tight_layout()
plt.show()
```

0%부터 시작하는 축에서는 두 막대의 높이 차이가 눈에 잘 띄지 않는다. 실제로 근소한 차이였으니 당연하다. 그런데 같은 두 숫자를, 축의 시작점만 33.5%로 옮겨서 다시 그리면 어떻게 될까.

```{python}
#| echo: false
#| fig-cap: "The same two numbers (36.0% vs 34.7%), redrawn with a y-axis starting at 33.5% — close to how the gap was shown on election-night broadcast graphics. The visual gap is now more than twice as large."
import matplotlib.pyplot as plt

candidates = ["Nam\n(36.0%)", "Kim\n(34.7%)"]
values = [36.0, 34.7]
colors = ["#1e3a2f", "#a8790a"]

fig, ax = plt.subplots(figsize=(6, 3.8))
bars = ax.bar(candidates, values, color=colors, width=0.45)
for b, v in zip(bars, values):
    ax.annotate(f"{v:.1f}%", xy=(b.get_x()+b.get_width()/2, v), xytext=(0, 5),
                textcoords="offset points", ha="center", fontsize=11)
ax.set_ylabel("Vote share (%)")
ax.set_ylim(33.5, 37)
ax.set_title("Axis starts at 33.5% — the same 1.3-point gap looks 2x bigger", fontsize=10.5, color="#1e3a2f")
ax.spines[["top", "right"]].set_visible(False)
plt.tight_layout()
plt.show()
```

숫자는 그대로 36.0과 34.7이다. 그런데 막대 높이의 비율은 완전히 달라졌다. 축을 0에서 시작하면 두 막대의 높이 비는 36.0:34.7, 거의 1:1이다. 축을 33.5%에서 시작하면 높이 비는 (36.0-33.5):(34.7-33.5) = 2.5:1.2, 약 2:1이 된다. **같은 데이터, 같은 숫자 라벨인데도 시각적으로는 두 배 차이 나는 그래프가 만들어진다.**

::: {.callout-warning}
## 이런 사례는 반복된다

절단된 축으로 격차를 부풀린 사례는 이 선거 방송 하나만이 아니다.

- **KBS (2014년 지방선거)**: 세종시장 후보 간 격차 0.7%포인트가 화면상 2배 이상 차이로, 경기도지사 격차 1.3%포인트도 마찬가지로 과장됐다.
- **채널A (2012년 대선)**: 9월 여론조사 격차(0.9%포인트)보다 10월 격차(0.3%포인트)가 오히려 더 크게 그려졌다.
- **JTBC 뉴스룸 (2017년)**: 대선 후보 지지율 격차 11.1%포인트가 그래프 높이 차이로는 훨씬 크게 표시됐다.

방송사마다, 선거마다 반복된다는 것은 이것이 어느 한 곳의 실수가 아니라 그래프 제작 과정에서 반복적으로 발생하는 구조적 함정이라는 뜻이다.
:::

## 축을 자르는 게 항상 거짓말은 아니다

여기서 중요한 균형점이 하나 있다. 축을 0에서 시작하지 않는 것 자체가 항상 잘못은 아니다. 체온(35~42도 사이에서만 의미가 있는 값), 주가지수, 혈압처럼 절대적인 0이 무의미하거나, 미세한 변화 자체가 중요한 정보인 데이터에서는 축을 확대해서 보여주는 쪽이 오히려 더 정확한 정보 전달일 수 있다. 체온 그래프를 0도부터 그린다면 36.5도와 39도의 차이가 눈에 보이지도 않을 것이다.

::: {.callout-tip}
## 그래프를 볼 때 3초 안에 확인할 것

1. **y축이 0에서 시작하는가?** 막대그래프라면 원칙적으로 그래야 높이가 값에 비례한다.
2. **0에서 시작하지 않는다면, 절단선(물결선·파선)이 표시되어 있는가?** 정직한 그래프는 축이 잘렸다는 사실을 시각적으로 알린다.
3. **막대 높이의 "느낌"과 라벨에 적힌 숫자의 "차이"가 비슷한가?** 둘이 어긋난다면, 그 어긋남의 크기만큼 그래프가 과장하고 있다는 뜻이다.
:::

선거 개표방송처럼 두 값을 순위·우열로 비교하려는 막대그래프에서는 이 원칙이 특히 중요하다. 절대적 우열이 아니라 근소한 차이일 뿐인데, 축을 자르는 순간 시청자의 머릿속에는 "압도적인 차이"라는 인상이 남는다.

## 결론: 숫자는 그대로여도, 그림은 거짓말할 수 있다

그래프 조작이라고 하면 흔히 숫자를 바꿔치기하는 것을 떠올리지만, 가장 흔하고 가장 눈에 띄지 않는 방법은 숫자는 그대로 둔 채 **축만 바꾸는 것**이다. 라벨에 적힌 36.0%와 34.7%는 거짓이 아니다. 다만 그 숫자를 담는 그릇, 즉 축의 눈금을 어떻게 설계했는지에 따라 같은 진실이 전혀 다른 인상으로 전달된다.

나는 학생들에게 이 캡처 화면을 보여줄 때마다 같은 말을 덧붙인다. "숫자를 믿기 전에, 먼저 축의 눈금부터 읽으세요." 막대의 높이에 속기는 쉽지만, 축의 시작점을 확인하는 3초는 누구나 할 수 있는 가장 간단한 방어법이다.

::: {.callout-note collapse="true"}
## 참고 자료

- [눈을 홀리는 '짝퉁 그래프'...의도적 실수? — 노컷뉴스](https://www.nocutnews.co.kr/news/5125487)
- [그래프 왜곡 — 나무위키](https://namu.wiki/w/%EA%B7%B8%EB%9E%98%ED%94%84%20%EC%99%9C%EA%B3%A1)
- [선거 여론조사의 시각화 자료를 제대로 이해하기 — 통계청 통계플러스](https://shi.kostat.go.kr/window/2024a/main/2024_sum_02.html)
:::
