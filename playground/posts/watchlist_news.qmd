---
title: "관심 종목 뉴스 검색기"
author: "권세혁"
date: "2026-09-08"
date-modified: "2026-09-08"
description: "코스피·코스닥 시가총액 상위 약 300종목의 뉴스를 매일 자동으로 수집해, 종목명으로 찾아볼 수 있게 해드립니다."
categories: [주식, 뉴스]
format:
  html:
    sidebar: playground
    toc: false
---

```{=html}
<style>
.wn-header {
  background: linear-gradient(135deg, #1a2a4a 0%, #2d4a8c 60%, #3a5aad 100%);
  color: #f5f0e8;
  padding: 1.4rem 2rem;
  border-radius: 10px;
  margin-bottom: 1.2rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  flex-wrap: wrap;
}
.wn-header .wn-title {
  font-size: 1.5rem;
  font-weight: 800;
  margin: 0 0 0.25rem;
}
.wn-header .wn-title span { color: #a2b8f0; }
.wn-header .wn-sub {
  font-size: 0.85rem;
  opacity: 0.85;
  margin: 0;
}
.wn-controls {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  font-size: 0.85rem;
}
.wn-controls select {
  font-size: 0.85rem;
  padding: 0.3rem 0.5rem;
  border-radius: 6px;
  border: none;
}
.wn-meta {
  font-size: 0.8rem;
  color: #888;
  margin: -0.6rem 0 1rem;
}

.wn-search-row input {
  width: 100%;
  max-width: 420px;
  font-size: 0.9rem;
  padding: 0.55rem 0.9rem;
  border-radius: 8px;
  border: 1px solid #ddd;
  margin-bottom: 0.6rem;
}
.wn-hint {
  font-size: 0.78rem;
  color: #999;
  margin: 0 0 0.9rem;
}
.wn-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 1.3rem;
  max-height: 12.5rem;
  overflow-y: auto;
}
.wn-chip {
  font-size: 0.85rem;
  padding: 0.4rem 0.9rem;
  border-radius: 999px;
  border: 1px solid #d7ddf0;
  background: #eef2fb;
  color: #2d4a8c;
  cursor: pointer;
}
.wn-chip .wn-chip-market {
  font-size: 0.68rem;
  color: #8a95b8;
  margin-left: 0.3rem;
}
.wn-chip:hover { background: #dfe6f7; }
.wn-chip.wn-active {
  background: #2d4a8c;
  color: #fff;
  border-color: #2d4a8c;
}
.wn-chip.wn-active .wn-chip-market { color: #cfd9f5; }
.wn-chip-empty {
  font-size: 0.85rem;
  color: #999;
}

.wn-result-head {
  font-size: 1rem;
  font-weight: 700;
  margin-bottom: 0.6rem;
  color: #1f3d33;
}
.wn-list {
  list-style: none;
  margin: 0;
  padding: 0;
  border: 1px solid #e5e5e5;
  border-radius: 10px;
  overflow: hidden;
}
.wn-item {
  padding: 0.85rem 1.1rem;
  border-bottom: 1px solid #f2f2f2;
  background: var(--bs-body-bg, #fff);
}
.wn-item:last-child { border-bottom: none; }
.wn-item h3 {
  margin: 0 0 0.25rem;
  font-size: 0.95rem;
  line-height: 1.4;
}
.wn-item h3 a {
  color: #1f3d33;
  text-decoration: none;
}
.wn-item h3 a:hover { text-decoration: underline; }
.wn-item-meta {
  font-size: 0.74rem;
  color: #999;
}
.wn-rank {
  font-weight: 700;
  color: #3a5aad;
  margin-right: 0.4rem;
}
.wn-error {
  color: #a33;
  font-size: 0.85rem;
  padding: 0.9rem 0;
}
.wn-empty {
  color: #999;
  font-size: 0.88rem;
}
.wn-footnote {
  margin-top: 1.4rem;
  font-size: 0.78rem;
  color: #999;
  line-height: 1.6;
}
</style>

<div class="wn-header">
  <div>
    <div class="wn-title">🔍 <span>관심 종목 뉴스 검색기</span></div>
    <p class="wn-sub">코스피·코스닥 시가총액 상위 약 300종목의 뉴스를 매일 자동 수집</p>
  </div>
  <div class="wn-controls">
    <label for="wn-date-select">날짜</label>
    <select id="wn-date-select"></select>
  </div>
</div>

<p class="wn-meta" id="wn-generated-at"></p>

<div class="wn-search-row">
  <input type="text" id="wn-search" placeholder="종목명 또는 종목코드로 검색 (예: 삼성전자, 005930, 에코프로)" autocomplete="off">
</div>
<p class="wn-hint" id="wn-hint"></p>

<div class="wn-chips" id="wn-chips">
  <p class="wn-empty">종목 목록을 불러오는 중입니다…</p>
</div>

<div id="wn-result">
  <p class="wn-empty">위에서 종목을 선택하면 해당 종목의 뉴스가 표시됩니다.</p>
</div>

<p class="wn-footnote">
  대상 종목은 코스피·코스닥 시가총액 랭킹을 매일 기준으로 다시 산출한 상위 약 300종목(코스피 200 + 코스닥 100)이며, 순위 변동에 따라 목록도 자동으로 갱신됩니다.
  이 페이지는 방문자가 아무 종목이나 실시간으로 검색하는 방식이 아니라, 위 범위에 포함된 종목의 뉴스만 매일 자동으로 미리 수집해 두는 방식입니다
  (정적 사이트 특성상 브라우저에서 외부 사이트를 실시간으로 검색하는 데는 제약이 있습니다). 범위 밖의 소형주는 검색되지 않을 수 있으며, 특정 종목을 꼭 넣고 싶다면 알려주시면 반영할 수 있습니다.
  뉴스는 <a href="https://finance.naver.com/item/main.naver?code=005930" target="_blank" rel="noopener">네이버 증권 종목 뉴스</a>에 노출되는 제목·날짜를 그대로 수집·정리한 것이며,
  본문은 각 언론사 원문 링크에서 확인할 수 있습니다. 본 정보는 참고용이며 투자 결정의 책임은 본인에게 있습니다.
</p>

<script>
(function () {
  const chips = document.getElementById("wn-chips");
  const result = document.getElementById("wn-result");
  const select = document.getElementById("wn-date-select");
  const meta = document.getElementById("wn-generated-at");
  const search = document.getElementById("wn-search");
  const hint = document.getElementById("wn-hint");
  const cache = new Map();
  let current = null;
  let activeCode = null;

  // A few common full/short name variants worth matching directly, on top
  // of plain substring search over the registered universe.
  const ALIASES = {
    "005380": ["현대차", "현대자동차"],
    "000270": ["기아", "기아자동차"],
    "005490": ["POSCO홀딩스", "포스코홀딩스", "포스코"],
    "035420": ["NAVER", "네이버"],
    "000660": ["SK하이닉스", "에스케이하이닉스"],
  };

  function escapeHtml(str) {
    return String(str).replace(/[&<>"']/g, (c) => ({
      "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
    }[c]));
  }

  function stockMatches(stock, q) {
    if (!q) return false;
    const candidates = [stock.name, stock.code, ...(ALIASES[stock.code] || [])];
    return candidates.some((c) => c.toLowerCase().includes(q));
  }

  function renderResult(stock) {
    if (!stock) {
      result.innerHTML = '<p class="wn-empty">위에서 종목을 선택하면 해당 종목의 뉴스가 표시됩니다.</p>';
      return;
    }
    if (stock.status === "error" || !stock.items || !stock.items.length) {
      result.innerHTML = `
        <div class="wn-result-head">${escapeHtml(stock.name)}</div>
        <p class="wn-error">${stock.status === "error" ? "수집 실패" + (stock.error ? ": " + escapeHtml(stock.error) : "") : "수집된 뉴스가 없습니다."}</p>`;
      return;
    }
    result.innerHTML = `
      <div class="wn-result-head">${escapeHtml(stock.name)} 뉴스</div>
      <ul class="wn-list">${stock.items.map((it) => `
        <li class="wn-item">
          <h3><span class="wn-rank">${it.rank}</span><a href="${escapeHtml(it.url)}" target="_blank" rel="noopener">${escapeHtml(it.title)}</a></h3>
          <div class="wn-item-meta">${escapeHtml(it.time || "")}</div>
        </li>`).join("")}</ul>`;
  }

  function renderChips(query) {
    const stocks = (current && current.stocks) || [];
    const q = (query || "").trim().toLowerCase();
    if (!q) {
      chips.innerHTML = "";
      hint.textContent = `등록된 ${stocks.length}개 종목 중 검색해보세요.`;
      return;
    }
    const filtered = stocks.filter((s) => stockMatches(s, q)).slice(0, 30);
    if (!filtered.length) {
      chips.innerHTML = '<p class="wn-chip-empty">일치하는 등록 종목이 없습니다.</p>';
      hint.textContent = "";
      return;
    }
    hint.textContent = `${filtered.length}개 종목이 검색되었습니다.`;
    chips.innerHTML = filtered.map((s) => `
      <button type="button" class="wn-chip${s.code === activeCode ? " wn-active" : ""}" data-code="${escapeHtml(s.code)}">${escapeHtml(s.name)}<span class="wn-chip-market">${escapeHtml(s.market === "KOSDAQ" ? "코스닥" : "코스피")}</span></button>
    `).join("");
    chips.querySelectorAll(".wn-chip").forEach((btn) => {
      btn.addEventListener("click", () => {
        activeCode = btn.dataset.code;
        renderChips(search.value);
        renderResult(stocks.find((s) => s.code === activeCode));
      });
    });
  }

  function renderSnapshot(snapshot) {
    current = snapshot;
    activeCode = null;
    meta.textContent = "생성 시각: " + (snapshot.generatedAt || snapshot.date);
    renderChips(search.value);
    renderResult(null);
  }

  async function loadSnapshot(date) {
    if (cache.has(date)) return cache.get(date);
    const res = await fetch(`../data/watchlist_news/${date}.json`);
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const data = await res.json();
    cache.set(date, data);
    return data;
  }

  async function onDateChange() {
    chips.innerHTML = '<p class="wn-empty">종목 목록을 불러오는 중입니다…</p>';
    result.innerHTML = "";
    try {
      const snapshot = await loadSnapshot(select.value);
      renderSnapshot(snapshot);
    } catch (err) {
      chips.innerHTML = `<p class="wn-error">이 날짜의 데이터를 불러오지 못했습니다: ${escapeHtml(err.message)}</p>`;
    }
  }

  search.addEventListener("input", () => renderChips(search.value));

  async function init() {
    try {
      const res = await fetch("../data/watchlist_news/manifest.json");
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const manifest = await res.json();
      const dates = manifest.dates || [];
      if (!dates.length) {
        chips.innerHTML = '<p class="wn-empty">아직 수집된 데이터가 없습니다. 첫 자동 수집은 매일 오전 8시 10분경 실행됩니다.</p>';
        return;
      }
      select.innerHTML = dates.map((d) => `<option value="${d}">${d}</option>`).join("");
      select.addEventListener("change", onDateChange);
      await onDateChange();
    } catch (err) {
      chips.innerHTML = `<p class="wn-error">데이터를 불러오지 못했습니다: ${escapeHtml(err.message)}</p>`;
    }
  }

  init();
})();
</script>
```
